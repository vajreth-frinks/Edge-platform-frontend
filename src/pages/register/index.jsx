import RegisterCard from "../../components/ui-parts/register-card";

const Register = () => {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <RegisterCard />
    </main>
  );
};

Register.propTypes = {};

export default Register;
